# Bug Report: Undeterministic Parameter Ordering in RPC Calls

**Commit:** 671c27dfa20ac16c595cdde9e4170a825d7e12f2
**Date:** January 18, 2026
**Severity:** Medium (intermittent but causes API failures)
**Status:** Fixed
**Affected Endpoints:** `/api/getrawtransaction`, `/api/verifymessage`, `/api/sendmany`

## Description

The eiquidus explorer's `/api/getrawtransaction`, `/api/verifymessage`, and `/api/sendmany` endpoints occasionally fail when query parameters are passed in non-alphabetical order. The daemon rejects the RPC call with:

```
Error: Wrong type passed:
{
    "Position 1 (txid)": "JSON value of type number is not of expected type string"
}
```

## Root Cause

In `lib/nodeapi.js`, `getrawtransaction`, `verifymessage`, and `sendmany` API endpoints used `Object.keys(query_parameters).forEach()` to iterate over query parameters. This caused parameters to be added to the RPC call in random order.

### getrawtransaction (2 parameters)
```javascript
case 'getrawtransaction':
  Object.keys(query_parameters).forEach(function(parameter) {
    if (parameter == 'txid')
      params.push(query_parameters[parameter]);
    else if (parameter == 'decrypt')
      params.push(parseInt(query_parameters[parameter]));
  });
```

### verifymessage (3 parameters, all strings)
Similar issue - parameters could be sent in wrong order causing silent verification failures.

### sendmany (Complex parameter collection)
```javascript
case 'sendmany':
  var after_account = false;
  var before_min_conf = true;
  var address_info = {};

  Object.keys(query_parameters).forEach(function(parameter) {
    if (parameter == 'minconf') {
      before_min_conf = false;
      params.push(address_info);
    }
    let param = query_parameters[parameter];
    if (!isNaN(param))
      param = parseFloat(param);
    if (after_account && before_min_conf)
      address_info[parameter] = param;  // Collects address:amount pairs
    else
      params.push(param);
    if (parameter == 'account')
      after_account = true;
  });
```

This logic **depends on iteration order**. Bitcoin's `sendmany` expects:
```
sendmany "account" {"addr1":amount, "addr2":amount} [minconf] [comment] [comment_to]
```

### Why This Failed

1. **`Object.keys()` doesn't guarantee order** - While modern JavaScript engines often maintain insertion order, this is not guaranteed across all environments and implementations
2. **HTTP query strings are unordered** - According to RFC 3986, `?txid=abc&decrypt=2` is semantically identical to `?decrypt=2&txid=abc`
3. **Proxies/CDNs may reorder** - Many reverse proxies alphabetically sort query parameters for caching efficiency
4. **Express parsing varies** - The order Express adds keys to `req.query` depends on how the URL string was received

When parameters were processed as `decrypt` before `txid`, the daemon received:
```javascript
getrawtransaction(1, "abc123...")  // Wrong!
```

But it expected:
```javascript
getrawtransaction("abc123...", 1)  // Correct
```

The daemon's parameter validation caught this and rejected the call.

## Why the Bug Was Intermittent

The bug only manifested when query parameters arrived in non-alphabetical order:

### Scenario A: Alphabetical Order (Always Fails)
```
URL:  /api/getrawtransaction?decrypt=2&txid=abc123
Parsed: {decrypt: "1", txid: "abc123"}
Keys:  ["decrypt", "txid"]
Params: [1, "abc123"]  ← ERROR: Position 1 is number, should be string
```

### Scenario B: Request Order (Works)
```
URL:  /api/getrawtransaction?txid=abc123&decrypt=2
Parsed: {txid: "abc123", decrypt: "1"}
Keys:  ["txid", "decrypt"]
Params: ["abc123", 1]  ← SUCCESS: Correct order
```

### Scenario C: Proxy/CDN Reordering
If a proxy or CDN alphabetically sorts parameters, requests that work in testing fail in production, or vice versa. This explains why the bug appeared suddenly or intermittently.

## Solution

Changed from unordered iteration to **explicit parameter handling in correct order**:

### Before:
```javascript
case 'getrawtransaction':
  Object.keys(query_parameters).forEach(function(parameter) {
    if (parameter == 'txid')
      params.push(query_parameters[parameter]);
    else if (parameter == 'decrypt')
      params.push(parseInt(query_parameters[parameter]));
  });

  var cmd = prepareRpcCommand(settings.api_cmds.getrawtransaction, params);
```

### After:
```javascript
case 'getrawtransaction':
  if (query_parameters.txid != null)
    params.push(query_parameters.txid);        // Always first
  if (query_parameters.decrypt != null)
    params.push(parseInt(query_parameters.decrypt)); // Always second

  var cmd = prepareRpcCommand(settings.api_cmds.getrawtransaction, params);
```

Also fixed `verifymessage` case with the same pattern:

```javascript
case 'verifymessage':
  if (query_parameters.address != null)
    params.push(query_parameters.address);
  if (query_parameters.signature != null) {
    let param = decodeURIComponent(query_parameters.signature);
    while (param.indexOf(' ') > -1)
      param = param.replace(' ', '+');
    params.push(param);
  }
  if (query_parameters.message != null)
    params.push(query_parameters.message);
```

Also fixed `sendmany` case which had complex parameter collection logic:

```javascript
case 'sendmany':
  var address_info = {};
  var account = null;
  var minconf = null;
  var comment = null;
  var comment_to = null;

  // Extract known parameters first
  if (query_parameters.account != null)
    account = query_parameters.account;
  if (query_parameters.minconf != null)
    minconf = parseInt(query_parameters.minconf);
  if (query_parameters.comment != null)
    comment = query_parameters.comment;
  if (query_parameters.comment_to != null)
    comment_to = query_parameters.comment_to;

  // Collect address:amount pairs (any parameter not in known list)
  var known_params = ['account', 'minconf', 'comment', 'comment_to'];
  Object.keys(query_parameters).forEach(function(parameter) {
    if (!known_params.includes(parameter)) {
      let param = query_parameters[parameter];
      if (!isNaN(param))
        param = parseFloat(param);
      address_info[parameter] = param;
    }
  });

  // Build params in correct order: account, address_info, minconf, comment, comment_to
  if (account != null)
    params.push(account);
  params.push(address_info);
  if (minconf != null)
    params.push(minconf);
  if (comment != null)
    params.push(comment);
  if (comment_to != null)
    params.push(comment_to);
```

## Why This Fix Works

1. **Deterministic order:** Explicit `if` statements guarantee parameters are added in the correct sequence
2. **Null safety:** Checking `!= null` prevents errors when optional parameters are missing
3. **Minimal change:** Preserves all existing logic while fixing the ordering issue
4. **Performance:** Same O(n) complexity for checking parameters, just deterministic
5. **Works regardless of input order:** No matter how the URL is constructed, how the browser sends it, how proxies reorder it, or how Express parses it, parameters are always sent to the daemon in the correct order

## Affected Endpoints

- `/api/getrawtransaction` - Fixed (2 parameters: txid, decrypt)
- `/api/verifymessage` - Fixed (3 parameters: address, signature, message)
- `/api/sendmany` - Fixed (Multiple parameters with complex address collection logic)

## Affected Coins

**This affects ALL coins using Bitcoin-compatible JSON-RPC.**

The issue is in **eiquidus code**, not the blockchain. Any coin using this explorer that:
- Has `getrawtransaction`, `verifymessage`, or `sendmany` API enabled
- Receives query parameters in varying order in URLs
- Has an RPC daemon that validates parameter types

Will experience this bug. The error message comes from the Bitcoin/Blackcoin daemon's parameter validation, but the root cause is in eiquidus's `lib/nodeapi.js`.

## Testing

To verify fix:
```bash
# Test getrawtransaction with alphabetical order (would fail before fix)
curl "http://localhost:3000/api/getrawtransaction?decrypt=2&txid=YOUR_TXID_HERE"

# Test getrawtransaction with reverse order (would work before fix)
curl "http://localhost:3000/api/getrawtransaction?txid=YOUR_TXID_HERE&decrypt=2"

# Test verifymessage
curl "http://localhost:3000/api/verifymessage?address=ADDR&signature=SIG&message=MSG"

# Test sendmany with various parameter orders
curl "http://localhost:3000/api/sendmany?account=YOUR_ACCOUNT&ADDR1=1.5&ADDR2=2.5&minconf=6"
curl "http://localhost:3000/api/sendmany?minconf=6&ADDR1=1.5&account=YOUR_ACCOUNT&ADDR2=2.5"

# All should now work correctly regardless of parameter order
```

## Files Changed

- `lib/nodeapi.js` - Fixed parameter ordering in `getrawtransaction`, `verifymessage`, and `sendmany` cases

## Related

None

## Reporter

Found and fixed during troubleshooting of intermittent API failures in production environment.
